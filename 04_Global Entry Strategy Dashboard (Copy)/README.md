
  # Global Entry Strategy Dashboard (Copy)

  This is a code bundle for Global Entry Strategy Dashboard (Copy). The original project is available at https://www.figma.com/design/ejLx43vYXrB4fcNrUgYA9D/Global-Entry-Strategy-Dashboard--Copy-.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  